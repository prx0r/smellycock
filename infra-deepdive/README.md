# infra-deepdive — the index (agent resolver for the deep-dive/plan docs)

*2026-08-15 · the deep-dive + plan pile, organized so an agent can find the right doc fast (AXIOM 8: dense,
one-concern-one-doc, MANIFEST-resolver). These are the actionable session notes that drive the build. Read
`00` first; then jump to the doc for your task.*

## READ THIS FIRST
| Doc | What it is |
|---|---|
| **00-OVERVIEW** | the whole system + the honest scorecard (real vs doc). START HERE. |
| **05-NEXT-AGENT-EXECUTION** | the prioritized execution path + what the next agent does. |

## ORIENTATION (understand the stack)
| Doc | What it is |
|---|---|
| 01-PATALAORG-REPOSITORY | the production docs repo, both lanes, validators |
| 02-PATALA-PIPELINE | the RAW→C1 translation pipeline (DAG, E2E, skills, MCP) |
| 03-HERMES-INFRASTRUCTURE | how to drive Hermes (profile, kanban, skills, MCP) |
| 04-IP-GRAPH-POST-C1-LANE | the post-C1 scholarship lane + gates |

## THE PLAN (what to build — the frontier)
| Doc | What it is |
|---|---|
| 06-PEER-REVIEW-FRONTIER | peer review grounded in LOGGED RUNS: what works, the gaps F1-F6 |
| 07-EXPANDED-PIPELINE-ACQUISITION | the full factorial acquisition→translation DAG (S1-S12) |
| 08-CANONICAL-ASSESS-FLOW | the deterministic decision engine (tag/state/format/route per work) |
| 09-MAKE-IT-NOT-BS-PLAN | the master plan to make the claimed stack real (A-E phases) |
| 10-QUICKEST-BUILD | the fastest gated path to the first real RAW→EDUCATION object |
| 11-BRAINSTORM-INTEGRATION | tool-to-layer mapping + depth-first strategy + experiment queue |

## THE BENCHMARK + PRODUCT
| Doc | What it is |
|---|---|
| 12-MODEL-COMPARISON | run different models on the same pipeline + compare quality/speed |
| 13-BENCHMARK-PROJECTOR-THESIS | the translation estimator (projector) + the full benchmark surface |

## THE ONE-LINE MAP
> **Understand (00-04) → plan the frontier (05-11) → build the benchmark/product (12-13). The truth is the
> logged runs (progress registry + e2e-trace + check.py), not the prose.**
