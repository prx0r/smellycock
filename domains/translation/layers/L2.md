# LAYER L2 — the guided translation

*The ARGMAP-guided translation: renders the L1 controlled reading into philosophically-grounded English
prose, carrying the ARGMAP's decision_for_l2 frame (the 0.118 fix).*

## Contract
| | |
|---|---|
| **Consumes** | L1 + ARGMAP (`decision_for_l2`) |
| **Produces** | L2 English prose |
| **Gate** | validator (L1 provenance + relaxed lemma-overlap for argmap-guided prose) |
| **Where** | `pipeline/l1_l2_worker.py` (`l2_generator_argmap_guided`) |

## Hermes call (per-layer logic)
- **`chat_agentic` + the `translate-reading` skill** — reads the real L1 + ARGMAP, renders grounded prose.

## Queue
- L1+ARGMAP-complete passages with no committed L2.

## Model / context
- `deepseek-v4-flash` · 1M context.

## Productivity signal
- avg time/call (~30-60s), committed/call, quality vs gold (the semantic scorer).
