# LAYER C1 — the scholarly commentary

*The final layer: turns the L200 audit into a scholarly commentary (summary + key terms). Batched.*

## Contract
| | |
|---|---|
| **Consumes** | L200 (the 8-section audit) |
| **Produces** | C1 commentary: summary + key_terms |
| **Gate** | C1-SPEC validator (4 sections, length, banned-lexicon) |
| **Where** | `pipeline/c1_worker.py` (`c1_generator_batched`) |

## Hermes call (per-layer logic)
- **plain `chat`** (batched JSONL).

## Queue
- L200-complete passages needing the commentary.

## Model / context
- `deepseek-v4-flash` · 1M context.

## Productivity signal
- avg time/call (~150s — the slowest layer), committed/call, quality vs gold (`raw-material/` IPVV C1).
