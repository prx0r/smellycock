# LAYER L200 — the bounded derivational audit (the moat)

*The 8-section derivational audit: classifies translation decisions (MT vs IA vs OPEN), fail-closed. A
bounded classifier, NOT open generation.*

## Contract
| | |
|---|---|
| **Consumes** | L2 (published) vs L1 (grounded) candidate pairs |
| **Produces** | 8-section audit (identification, derivation_map, MT decisions, open items, review_state) |
| **Gate** | fail-closed classifier (IGNORE default prior; GENERATION_FAILED → no commit) |
| **Where** | `pipeline/l200_worker.py` |

## Hermes call (per-layer logic)
- **plain `chat`** — the bounded classifier (constrained classification, IGNORE default).

## Queue
- L2-complete passages needing the audit.

## Model / context
- `deepseek-v4-flash`.

## Productivity signal
- avg time/call (~12-45s), committed/call, failure rate (the transient classifier hiccups).
