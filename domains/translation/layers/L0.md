# LAYER L0 — the deterministic philological floor

*The RAW-L0 deterministic layer: Vidyut segments the verse into tokens/lemmas. NO Hermes call — the
"quick deterministic process that passes between." Free-draining (never consumes the model budget).*

## Contract
| | |
|---|---|
| **Consumes** | SOURCE verse |
| **Produces** | L0 tokens/lemmas (structured) |
| **Type** | **deterministic** (Vidyut RAW-L0) |
| **Where** | `pipeline/l0_worker.py` |

## Hermes call
- **None.** Instant. ~185 objects per pass (free-draining in the factory).

## Queue
- SOURCE passages needing tokenization (drains immediately, no model budget).

## Model / context
- N/A (no model).

## Productivity signal
- Count committed/pass (it's the fast, free stage — the "easy layer").
