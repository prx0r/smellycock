# products/ — the epistemic + education-organism product modules

*The 25-product epistemic layer + education-organism products are built and run on **server2** (the
post-C1 lane). This box (`patalaorg`) is the shared production reference repo; the actual product modules
reside on the server2 worktree + are tracked in `runs/server2-post-c1-spine/`. See the epistemic docs in
`domains/epistemic/` for the product contracts; see `runs/server2-post-c1-spine/` for the build + run logs.*
